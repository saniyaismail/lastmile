apiVersion: apps/v1
kind: Deployment
metadata:
  name: location-service
  namespace: lastmile
spec:
  replicas: 1
  selector:
    matchLabels:
      app: location-service
  template:
    metadata:
      labels:
        app: location-service
    spec:
      containers:
      - name: location-service
        image: saniyaismail999/lastmile-location:1.0
        env:
        - name: DATABASE_URL
          value: "postgresql://lastmile:lastmile@postgres:5432/lastmile"
        - name: RABBITMQ_URL
          value: "amqp://guest:guest@rabbitmq:5672/"
        ports:
        - containerPort: 50053
---
apiVersion: v1
kind: Service
metadata:
  name: location-service
  namespace: lastmile
spec:
  selector:
    app: location-service
  ports:
  - port: 50053
    targetPort: 50053
